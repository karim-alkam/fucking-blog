
export const metadata = {
    title: 'COMM_LINK',
    description: 'Transmit your message to the system.',
};

export default function ContactLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
